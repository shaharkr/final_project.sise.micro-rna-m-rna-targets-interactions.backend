from setuptools import setup, find_packages

setup(
    name='mirna_pipeline',
    version='1.0',
    packages=find_packages(),
    description='Pipeline for extract interactions from chimera papers',
    author='Gilad Ben Or',
    author_email='benorgi@post.bgu.ac.il',
    include_package_data=True,
    install_requires=[

    ],

)
